# Prompts

## Coach report (v0.1)
- Prompt version: `v0.1`
- Schema version: `v0.1`
- Payload: compact JSON with game metadata + engine critical moments only.
- Output: structured JSON report with citations to `move_id` and `fen_hash`.
